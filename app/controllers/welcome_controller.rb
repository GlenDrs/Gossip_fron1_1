class WelcomeController < ApplicationController
  def show
   # @welcome = User.find(params[:first_name])
=begin   
   @name = User.find(1).last_name
   @potin= User.find(1).description
=end

  end
end
